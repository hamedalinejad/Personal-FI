{
  "name": "personal-fi",
  "private": true,
  "type": "module",
  "description": "Personal-FI \u2014 Core engines bootstrap (docs + src skeleton)",
  "scripts": {
    "test": "node --test src/core/money/*.test.js src/core/costBasis/*.test.js src/core/fixtures/*.test.js src/core/domain/**/*.test.js src/core/persistence/*.test.js src/core/db/*.test.js",
    "lint": "node -e \"console.log('ESLint: enable when feature packages land (B-007)');\"",
    "docs:check-refs": "node -e \"console.log('docs-only checks manual');\"",
    "schema:smoke": "node scripts/schema-drift-smoke.js"
  },
  "dependencies": {
    "decimal.js": "^10.4.3"
  }
}
