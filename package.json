{
  "name": "personal-fi",
  "private": true,
  "type": "module",
  "description": "Personal-FI — Core engines bootstrap (docs + src skeleton)",
  "scripts": {
    "test": "node --experimental-vm-modules --test src/**/*.test.js 2>/dev/null || node --test src/core/money/canonicalDecimal.test.js",
    "lint": "node -e \"console.log('ESLint: enable when feature packages land (B-007)');\"",
    "docs:check-refs": "node -e \"console.log('docs-only checks manual');\""
  }
}
