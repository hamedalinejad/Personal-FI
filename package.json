{
  "name": "personal-fi",
  "private": true,
  "description": "Documentation-first phase \u2014 implementation lives on a future branch",
  "scripts": {
    "docs:check-refs": "node -e \"console.log('docs-only phase: run link checks manually; implementation branch will restore CI tests');\"",
    "lint": "node -e \"console.log('B-007: ESLint deferred until src/features packages exist on implementation branch');\""
  }
}
