{
  "name": "personal-fi",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:fixtures": "vitest run src/core/fixtures",
    "test:unit": "vitest run",
    "test:architecture": "vitest run src/architecture",
    "lint": "node -e \"console.log('lint: ESLint deferred until Feature source packages exist; boundary rules live in docs/core/Feature-Package-Architecture.md');\"",
    "test": "npm run test:unit",
    "test:coverage-gate": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "decimal.js": "^10.6.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
