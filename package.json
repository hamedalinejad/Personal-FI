{
  "name": "personal-fi",
  "private": true,
  "type": "module",
  "description": "Personal-FI \u2014 Core engines bootstrap (docs + src skeleton)",
  "scripts": {
    "test": "node --test src/core/money/*.test.js src/core/iran/*.test.js src/core/valuation/*.test.js src/core/costBasis/*.test.js src/core/fixtures/*.test.js src/core/domain/**/*.test.js src/core/persistence/*.test.js src/core/db/*.test.js src/features/**/*.test.js src/features/**/tests/*.test.js",
    "lint": "node scripts/lint-boundaries.js",
    "docs:check-refs": "node scripts/docs-check-refs.js",
    "schema:smoke": "node scripts/schema-drift-smoke.js",
    "schema:drift": "node scripts/schema-drift-test.js",
    "schema:inventory": "node scripts/field-inventory-verify.js",
    "schema:manifest": "node scripts/schema-manifest.js",
    "dep:graph": "node scripts/dependency-graph-check.js",
    "docs:validate": "node scripts/docs-validator.js",
    "registry:index": "node scripts/registry-index.js",
    "bench:smoke": "node scripts/bench-smoke.js",
    "gates": "npm test && npm run dep:graph && npm run docs:validate && npm run lint && npm run schema:drift && npm run schema:inventory && npm run registry:index && npm run bench:smoke"
  },
  "dependencies": {
    "decimal.js": "^10.4.3"
  }
}
