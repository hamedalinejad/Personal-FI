# Development (owner)

```
one defect → one code fix → one test → update QUALITY-STATUS.md
```
Do not create new BUG-*-STATUS micro-docs.

Gates: `npm run gates`
Coding order: Loan vertical RELEASE-PROVEN first, then copy pattern.
