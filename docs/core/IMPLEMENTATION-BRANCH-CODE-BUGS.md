# Code bug registry → permanent invariants

Historical P0-CODE / P1-CODE items are **closed as permanent regressions**.

See **`BUG-CODE-REGRESSION-INVARIANTS.md`** and tests under `src/core/**/**/*.test.js`.

Do not re-open as “missing on docs branch” — enforce via CI tests when engines expand.
