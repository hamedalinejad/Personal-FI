# Documentation Lifecycle (Audit §16)

**DELETE NOW = NONE**

Delete a file only when:
1. unique rule count = 0
2. inbound references = 0

Historical/P0-named files must be marked `CLOSED_HISTORICAL` after references move to concept homes — not mass-deleted by name pattern.

See `DOC-CONSOLIDATION-POLICY.md` if present.
